State #0

S → ·PROGRAM	(for symbol: [_EOI_])
PROGRAM → ·LINEBREAKCOMPLETE_COMMANDSLINEBREAK	(for symbol: [_EOI_])
PROGRAM → ·LINEBREAK	(for symbol: [_EOI_])
NEWLINE_LIST → ·TERM_LINE	(for symbol: [TERM_LINE])
NEWLINE_LIST → ·TERM_LINE	(for symbol: [<])
NEWLINE_LIST → ·TERM_LINE	(for symbol: [>])
NEWLINE_LIST → ·TERM_LINE	(for symbol: [(])
NEWLINE_LIST → ·TERM_LINE	(for symbol: [{])
NEWLINE_LIST → ·TERM_LINE	(for symbol: [!])
NEWLINE_LIST → ·TERM_LINE	(for symbol: [<<])
NEWLINE_LIST → ·TERM_LINE	(for symbol: [>>])
NEWLINE_LIST → ·TERM_LINE	(for symbol: [<&])
NEWLINE_LIST → ·TERM_LINE	(for symbol: [>&])
NEWLINE_LIST → ·TERM_LINE	(for symbol: [<>])
NEWLINE_LIST → ·TERM_LINE	(for symbol: [<<-])
NEWLINE_LIST → ·TERM_LINE	(for symbol: [>|])
NEWLINE_LIST → ·TERM_LINE	(for symbol: [if])
NEWLINE_LIST → ·TERM_LINE	(for symbol: [case])
NEWLINE_LIST → ·TERM_LINE	(for symbol: [while])
NEWLINE_LIST → ·TERM_LINE	(for symbol: [until])
NEWLINE_LIST → ·TERM_LINE	(for symbol: [for])
NEWLINE_LIST → ·TERM_LINE	(for symbol: [TERM_WORD])
NEWLINE_LIST → ·TERM_LINE	(for symbol: [TERM_ASSIGNMENT_WORD])
NEWLINE_LIST → ·TERM_LINE	(for symbol: [TERM_NAME])
NEWLINE_LIST → ·TERM_LINE	(for symbol: [TERM_IO_NUMBER])
NEWLINE_LIST → ·TERM_LINE	(for symbol: [_EOI_])
NEWLINE_LIST → ·NEWLINE_LISTTERM_LINE	(for symbol: [TERM_LINE])
NEWLINE_LIST → ·NEWLINE_LISTTERM_LINE	(for symbol: [<])
NEWLINE_LIST → ·NEWLINE_LISTTERM_LINE	(for symbol: [>])
NEWLINE_LIST → ·NEWLINE_LISTTERM_LINE	(for symbol: [(])
NEWLINE_LIST → ·NEWLINE_LISTTERM_LINE	(for symbol: [{])
NEWLINE_LIST → ·NEWLINE_LISTTERM_LINE	(for symbol: [!])
NEWLINE_LIST → ·NEWLINE_LISTTERM_LINE	(for symbol: [<<])
NEWLINE_LIST → ·NEWLINE_LISTTERM_LINE	(for symbol: [>>])
NEWLINE_LIST → ·NEWLINE_LISTTERM_LINE	(for symbol: [<&])
NEWLINE_LIST → ·NEWLINE_LISTTERM_LINE	(for symbol: [>&])
NEWLINE_LIST → ·NEWLINE_LISTTERM_LINE	(for symbol: [<>])
NEWLINE_LIST → ·NEWLINE_LISTTERM_LINE	(for symbol: [<<-])
NEWLINE_LIST → ·NEWLINE_LISTTERM_LINE	(for symbol: [>|])
NEWLINE_LIST → ·NEWLINE_LISTTERM_LINE	(for symbol: [if])
NEWLINE_LIST → ·NEWLINE_LISTTERM_LINE	(for symbol: [case])
NEWLINE_LIST → ·NEWLINE_LISTTERM_LINE	(for symbol: [while])
NEWLINE_LIST → ·NEWLINE_LISTTERM_LINE	(for symbol: [until])
NEWLINE_LIST → ·NEWLINE_LISTTERM_LINE	(for symbol: [for])
NEWLINE_LIST → ·NEWLINE_LISTTERM_LINE	(for symbol: [TERM_WORD])
NEWLINE_LIST → ·NEWLINE_LISTTERM_LINE	(for symbol: [TERM_ASSIGNMENT_WORD])
NEWLINE_LIST → ·NEWLINE_LISTTERM_LINE	(for symbol: [TERM_NAME])
NEWLINE_LIST → ·NEWLINE_LISTTERM_LINE	(for symbol: [TERM_IO_NUMBER])
NEWLINE_LIST → ·NEWLINE_LISTTERM_LINE	(for symbol: [_EOI_])
LINEBREAK → ·NEWLINE_LIST	(for symbol: [_EOI_])
LINEBREAK → ·NEWLINE_LIST	(for symbol: [TERM_IO_NUMBER])
LINEBREAK → ·NEWLINE_LIST	(for symbol: [TERM_NAME])
LINEBREAK → ·NEWLINE_LIST	(for symbol: [TERM_ASSIGNMENT_WORD])
LINEBREAK → ·NEWLINE_LIST	(for symbol: [TERM_WORD])
LINEBREAK → ·NEWLINE_LIST	(for symbol: [for])
LINEBREAK → ·NEWLINE_LIST	(for symbol: [until])
LINEBREAK → ·NEWLINE_LIST	(for symbol: [while])
LINEBREAK → ·NEWLINE_LIST	(for symbol: [case])
LINEBREAK → ·NEWLINE_LIST	(for symbol: [if])
LINEBREAK → ·NEWLINE_LIST	(for symbol: [>|])
LINEBREAK → ·NEWLINE_LIST	(for symbol: [<<-])
LINEBREAK → ·NEWLINE_LIST	(for symbol: [<>])
LINEBREAK → ·NEWLINE_LIST	(for symbol: [>&])
LINEBREAK → ·NEWLINE_LIST	(for symbol: [<&])
LINEBREAK → ·NEWLINE_LIST	(for symbol: [>>])
LINEBREAK → ·NEWLINE_LIST	(for symbol: [<<])
LINEBREAK → ·NEWLINE_LIST	(for symbol: [!])
LINEBREAK → ·NEWLINE_LIST	(for symbol: [{])
LINEBREAK → ·NEWLINE_LIST	(for symbol: [(])
LINEBREAK → ·NEWLINE_LIST	(for symbol: [>])
LINEBREAK → ·NEWLINE_LIST	(for symbol: [<])
LINEBREAK → ·	(for symbol: [_EOI_])
LINEBREAK → ·	(for symbol: [TERM_IO_NUMBER])
LINEBREAK → ·	(for symbol: [TERM_NAME])
LINEBREAK → ·	(for symbol: [TERM_ASSIGNMENT_WORD])
LINEBREAK → ·	(for symbol: [TERM_WORD])
LINEBREAK → ·	(for symbol: [for])
LINEBREAK → ·	(for symbol: [until])
LINEBREAK → ·	(for symbol: [while])
LINEBREAK → ·	(for symbol: [case])
LINEBREAK → ·	(for symbol: [if])
LINEBREAK → ·	(for symbol: [>|])
LINEBREAK → ·	(for symbol: [<<-])
LINEBREAK → ·	(for symbol: [<>])
LINEBREAK → ·	(for symbol: [>&])
LINEBREAK → ·	(for symbol: [<&])
LINEBREAK → ·	(for symbol: [>>])
LINEBREAK → ·	(for symbol: [<<])
LINEBREAK → ·	(for symbol: [!])
LINEBREAK → ·	(for symbol: [{])
LINEBREAK → ·	(for symbol: [(])
LINEBREAK → ·	(for symbol: [>])
LINEBREAK → ·	(for symbol: [<])

State transitions:

NEWLINE_LIST → State #4

NEWLINE_LIST → NEWLINE_LIST·TERM_LINE	(for symbol: [TERM_LINE])
NEWLINE_LIST → NEWLINE_LIST·TERM_LINE	(for symbol: [<])
NEWLINE_LIST → NEWLINE_LIST·TERM_LINE	(for symbol: [>])
NEWLINE_LIST → NEWLINE_LIST·TERM_LINE	(for symbol: [(])
NEWLINE_LIST → NEWLINE_LIST·TERM_LINE	(for symbol: [{])
NEWLINE_LIST → NEWLINE_LIST·TERM_LINE	(for symbol: [!])
NEWLINE_LIST → NEWLINE_LIST·TERM_LINE	(for symbol: [<<])
NEWLINE_LIST → NEWLINE_LIST·TERM_LINE	(for symbol: [>>])
NEWLINE_LIST → NEWLINE_LIST·TERM_LINE	(for symbol: [<&])
NEWLINE_LIST → NEWLINE_LIST·TERM_LINE	(for symbol: [>&])
NEWLINE_LIST → NEWLINE_LIST·TERM_LINE	(for symbol: [<>])
NEWLINE_LIST → NEWLINE_LIST·TERM_LINE	(for symbol: [<<-])
NEWLINE_LIST → NEWLINE_LIST·TERM_LINE	(for symbol: [>|])
NEWLINE_LIST → NEWLINE_LIST·TERM_LINE	(for symbol: [if])
NEWLINE_LIST → NEWLINE_LIST·TERM_LINE	(for symbol: [case])
NEWLINE_LIST → NEWLINE_LIST·TERM_LINE	(for symbol: [while])
NEWLINE_LIST → NEWLINE_LIST·TERM_LINE	(for symbol: [until])
NEWLINE_LIST → NEWLINE_LIST·TERM_LINE	(for symbol: [for])
NEWLINE_LIST → NEWLINE_LIST·TERM_LINE	(for symbol: [TERM_WORD])
NEWLINE_LIST → NEWLINE_LIST·TERM_LINE	(for symbol: [TERM_ASSIGNMENT_WORD])
NEWLINE_LIST → NEWLINE_LIST·TERM_LINE	(for symbol: [TERM_NAME])
NEWLINE_LIST → NEWLINE_LIST·TERM_LINE	(for symbol: [TERM_IO_NUMBER])
NEWLINE_LIST → NEWLINE_LIST·TERM_LINE	(for symbol: [_EOI_])
LINEBREAK → NEWLINE_LIST·	(for symbol: [_EOI_])
LINEBREAK → NEWLINE_LIST·	(for symbol: [TERM_IO_NUMBER])
LINEBREAK → NEWLINE_LIST·	(for symbol: [TERM_NAME])
LINEBREAK → NEWLINE_LIST·	(for symbol: [TERM_ASSIGNMENT_WORD])
LINEBREAK → NEWLINE_LIST·	(for symbol: [TERM_WORD])
LINEBREAK → NEWLINE_LIST·	(for symbol: [for])
LINEBREAK → NEWLINE_LIST·	(for symbol: [until])
LINEBREAK → NEWLINE_LIST·	(for symbol: [while])
LINEBREAK → NEWLINE_LIST·	(for symbol: [case])
LINEBREAK → NEWLINE_LIST·	(for symbol: [if])
LINEBREAK → NEWLINE_LIST·	(for symbol: [>|])
LINEBREAK → NEWLINE_LIST·	(for symbol: [<<-])
LINEBREAK → NEWLINE_LIST·	(for symbol: [<>])
LINEBREAK → NEWLINE_LIST·	(for symbol: [>&])
LINEBREAK → NEWLINE_LIST·	(for symbol: [<&])
LINEBREAK → NEWLINE_LIST·	(for symbol: [>>])
LINEBREAK → NEWLINE_LIST·	(for symbol: [<<])
LINEBREAK → NEWLINE_LIST·	(for symbol: [!])
LINEBREAK → NEWLINE_LIST·	(for symbol: [{])
LINEBREAK → NEWLINE_LIST·	(for symbol: [(])
LINEBREAK → NEWLINE_LIST·	(for symbol: [>])
LINEBREAK → NEWLINE_LIST·	(for symbol: [<])
TERM_LINE → State #3

NEWLINE_LIST → TERM_LINE·	(for symbol: [TERM_LINE])
NEWLINE_LIST → TERM_LINE·	(for symbol: [<])
NEWLINE_LIST → TERM_LINE·	(for symbol: [>])
NEWLINE_LIST → TERM_LINE·	(for symbol: [(])
NEWLINE_LIST → TERM_LINE·	(for symbol: [{])
NEWLINE_LIST → TERM_LINE·	(for symbol: [!])
NEWLINE_LIST → TERM_LINE·	(for symbol: [<<])
NEWLINE_LIST → TERM_LINE·	(for symbol: [>>])
NEWLINE_LIST → TERM_LINE·	(for symbol: [<&])
NEWLINE_LIST → TERM_LINE·	(for symbol: [>&])
NEWLINE_LIST → TERM_LINE·	(for symbol: [<>])
NEWLINE_LIST → TERM_LINE·	(for symbol: [<<-])
NEWLINE_LIST → TERM_LINE·	(for symbol: [>|])
NEWLINE_LIST → TERM_LINE·	(for symbol: [if])
NEWLINE_LIST → TERM_LINE·	(for symbol: [case])
NEWLINE_LIST → TERM_LINE·	(for symbol: [while])
NEWLINE_LIST → TERM_LINE·	(for symbol: [until])
NEWLINE_LIST → TERM_LINE·	(for symbol: [for])
NEWLINE_LIST → TERM_LINE·	(for symbol: [TERM_WORD])
NEWLINE_LIST → TERM_LINE·	(for symbol: [TERM_ASSIGNMENT_WORD])
NEWLINE_LIST → TERM_LINE·	(for symbol: [TERM_NAME])
NEWLINE_LIST → TERM_LINE·	(for symbol: [TERM_IO_NUMBER])
NEWLINE_LIST → TERM_LINE·	(for symbol: [_EOI_])
LINEBREAK → State #2

PROGRAM → LINEBREAK·COMPLETE_COMMANDSLINEBREAK	(for symbol: [_EOI_])
PROGRAM → LINEBREAK·	(for symbol: [_EOI_])
PROGRAM → State #1

S → PROGRAM·	(for symbol: [_EOI_])
